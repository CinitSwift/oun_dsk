import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.cinitswift.oun',
  appName: 'Oun TV',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
